# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/target/
recording_*
.crush/

# Auto Claude data directory
.auto-claude/

# ML models — large binary files, downloaded by CI (see build.yml)
src-tauri/resources/models/*.onnx

# JS lockfile — project uses bun (bun.lock is the lockfile)
package-lock.json

# Harness durable layer
harness.db
harness.db-wal
harness.db-shm
scripts/bin/harness-cli
scripts/bin/harness-cli.exe

# >>> maestro >>>
# Maestro local agent settings
.claude/settings.local.json
.codex/hooks.json
# <<< maestro <<<
